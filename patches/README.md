# Patches Directory

Place kernel patches here for automatic application during build.

## Patch Naming Convention

- `<description>.patch` - General patches
- `resksu_manual_hooks.patch` - ReSukiSU manual hooks patch

## Usage

Patches in this directory will be automatically detected and applied during the build process if they match specific KSU types.
